package Arrays;

import java.util.Arrays;
//Arrays class is utility class (all methods are static

public class ToString {
    public static void main (String[] args){
        int [] arr = {10,20,30,40,50};
        System.out.println(Arrays.toString(arr));//toString is overloaded method
    }
}
